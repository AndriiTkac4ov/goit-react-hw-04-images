import React from 'react';
import ReactDOM from 'react-dom/client';
import { AppByHooks } from 'components/App/AppByHooks';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppByHooks />
  </React.StrictMode>
);
